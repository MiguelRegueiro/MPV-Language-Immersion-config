# Autoplay next video in the playlist
loop-playlist=yes

# Set maximum volume to 380%
volume-max=380
# Save the playback position when closing and resume on reopening
save-position-on-quit=yes





 # Automatically load subtitle files
sub-auto=fuzzy

# Preferred subtitle languages (set your preferred order)
slang=jpn,zh,rus,por,en

# Subtitle font size (adjust as necessary)
sub-font-size=50

# Set default subtitle font (optional, adjust as needed)
sub-font="Arial"

# Subtitle style (color, shadow, etc.)
sub-color="#FFFFFF"
sub-shadow-color="#000000"
sub-border-size=2
sub-shadow-offset=2

# Keep subtitles within the video frame (useful for content with unusual aspect ratios)
sub-ass-vsfilter-aspect-compat=no




# Preferred audio languages (set your preferred order)
alang=jpn,zh,rus,por,en




# Enable hardware video decoding (if supported by your system)
hwdec=auto

# Improve video scaling quality (useful for higher resolution videos)
scale=ewa_lanczossharp
cscale=ewa_lanczossharp

# Enable interpolation (smooth playback for high frame rate videos)
interpolation=yes
tscale=oversample





# Enable OSC (On-Screen Controller) for better UI controls
osc=yes


# Enable auto fullscreen when playing a video
fullscreen=yes






# Resume playback on quit
save-position-on-quit=yes



# Autoplay next video in the playlist
loop-playlist=yes

# Set maximum volume to 380%
volume-max=380

# Save the playback position when closing and resume on reopening
save-position-on-quit=yes

# Automatically load subtitle files
sub-auto=fuzzy

# Preferred subtitle languages (set your preferred order)
slang=jpn,zh,en,rus,por

# Subtitle font size
sub-font-size=50

# Set default subtitle font (optional)
sub-font="Arial"

# Subtitle style (color, shadow, etc.)
sub-color="#FFFFFF"
sub-shadow-color="#000000"
sub-border-size=2
sub-shadow-offset=2

# Keep subtitles within the video frame
sub-ass-vsfilter-aspect-compat=no

# Preferred audio languages
alang=jpn,zh,rus,por,en

# Enable hardware video decoding
hwdec=auto

# Improve video scaling quality
scale=ewa_lanczossharp
cscale=ewa_lanczossharp

# Enable interpolation for smoother playback
interpolation=yes
tscale=oversample

# Enable On-Screen Controller (OSC)
osc=yes

# Auto fullscreen when playing a video
fullscreen=yes

# Screenshot template with timestamp
screenshot-template="~/desktop/mpv_screenshots/%F_%p_%02n"

